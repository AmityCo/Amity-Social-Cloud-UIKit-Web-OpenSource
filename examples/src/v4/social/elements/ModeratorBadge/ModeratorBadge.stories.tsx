import React from 'react';

import { ModeratorBadge } from './ModeratorBadge';

export default {
  title: 'v4-social/elements/ModeratorBadge',
};

export const ModeratorBadgeStory = {
  render: () => {
    return <ModeratorBadge />;
  },

  name: 'ModeratorBadge',
};
