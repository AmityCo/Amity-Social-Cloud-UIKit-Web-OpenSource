export type Interest = {
  description: string;
  name: string;
  emoji: string;
};

export type Community = {
  id: string;
  name: string;
  description: string;
  membersCount: number;
  avatarUrl: string;
};
