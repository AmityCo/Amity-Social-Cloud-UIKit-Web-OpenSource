import React from 'react';

import Search from '.';

export default {
    title: 'SDK Connected/Social/Pages',
};

export const SDKSearch = (props) => <Search {...props} />;

SDKSearch.storyName = 'Community & Search';